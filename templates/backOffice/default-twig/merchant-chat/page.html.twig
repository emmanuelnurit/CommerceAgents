{% extends '@BackOfficeDefaultTwig/base.html.twig' %}

{% block title %}{{ 'Merchant Agent'|trans({}, 'commerceagents') }}{% endblock %}
{% block body_testid %}commerceagents-merchant-chat{% endblock %}

{% block breadcrumb %}
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('admin.home') }}">{{ 'Home'|trans({}, 'commerceagents') }}</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{ 'Merchant Agent'|trans({}, 'commerceagents') }}</li>
        </ol>
    </nav>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ module_asset('CommerceAgents', 'assets/css/merchant-chat.css') }}">
{% endblock %}

{% block content %}
    <div class="container-fluid" data-testid="merchant-chat-page">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h1 class="h3 mb-0">{{ 'Merchant Agent'|trans({}, 'commerceagents') }}</h1>
            <div class="d-flex gap-2">
                <a class="btn btn-outline-secondary" href="{{ path('commerceagents_mcp_page') }}"><i class="bi bi-plug me-1" aria-hidden="true"></i>{{ 'MCP server'|trans({}, 'commerceagents') }}</a>
                <a class="btn btn-outline-primary" href="{{ path('commerceagents_changes') }}">{{ 'Proposed changes'|trans({}, 'commerceagents') }}</a>
            </div>
        </div>

        {% if not apiKeyConfigured %}
            <div class="alert alert-warning">
                {{ 'No LLM API key configured yet. Set one in the module configuration.'|trans({}, 'commerceagents') }}
                <a href="{{ path('admin.module.configure', {module_code: 'CommerceAgents'}) }}">{{ 'Configure'|trans({}, 'commerceagents') }}</a>
            </div>
        {% endif %}

        <div id="merchant-chat" class="card" data-endpoint="{{ path('commerceagents_merchant_chat') }}"
             data-i18n-connection-lost="{{ 'Connection lost'|trans({}, 'commerceagents') }}"
             data-i18n-service-unavailable="{{ 'Service unavailable'|trans({}, 'commerceagents') }}"
             data-i18n-error="{{ 'Something went wrong'|trans({}, 'commerceagents') }}"
             data-i18n-hidden="{{ 'Hidden'|trans({}, 'commerceagents') }}"
             data-i18n-stock="{{ 'Stock'|trans({}, 'commerceagents') }}">
            <div class="card-body mc-messages" id="mc-messages" aria-live="polite">
                <div class="mc-suggestions" id="mc-suggestions">
                    <p class="fw-semibold mb-2">{{ 'Quick actions'|trans({}, 'commerceagents') }}</p>
                    <button type="button" class="btn btn-sm btn-outline-secondary mc-suggestion">{{ 'Which products are almost out of stock?'|trans({}, 'commerceagents') }}</button>
                    <button type="button" class="btn btn-sm btn-outline-secondary mc-suggestion">{{ 'What should I take care of this morning?'|trans({}, 'commerceagents') }}</button>
                    <button type="button" class="btn btn-sm btn-outline-secondary mc-suggestion">{{ 'Revenue and top sellers over the last 7 days'|trans({}, 'commerceagents') }}</button>
                    <button type="button" class="btn btn-sm btn-outline-secondary mc-suggestion">{{ 'Which campaigns are currently active?'|trans({}, 'commerceagents') }}</button>
                </div>
            </div>
            <div class="card-footer">
                <form id="mc-form" class="d-flex gap-2">
                    <input type="text" id="mc-input" class="form-control" maxlength="2000"
                           placeholder="{{ 'Ask about sales, inventory, pricing…'|trans({}, 'commerceagents') }}" autocomplete="off">
                    <button type="submit" id="mc-send" class="btn btn-primary">{{ 'Send'|trans({}, 'commerceagents') }}</button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}

{% block scripts_extra %}
    <script src="{{ module_asset('CommerceAgents', 'assets/js/chat-markdown.js') }}"></script>
    <script src="{{ module_asset('CommerceAgents', 'assets/js/product-cards.js') }}"></script>
    <script src="{{ module_asset('CommerceAgents', 'assets/js/merchant-chat.js') }}"></script>
{% endblock %}
